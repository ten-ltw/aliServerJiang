version: '3.8'

services:
  shangji:
    image: shangji:latest
    container_name: shangji
    restart: always
    volumes:
      - ./data:/app/data  # ✅ 映射 data 目录
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
